# ============================================================
# NCKH - Gold Price Prediction (TDTU 2025-2026)
# Requirements pinned per Architecture v0.1
# Python 3.11
# ============================================================

# ----- Core -----
pandas==2.2.3
numpy==1.26.4
scipy==1.14.1
pyarrow==17.0.0
pyyaml==6.0.2
tqdm==4.66.5
python-dateutil==2.9.0.post0

# ----- Data fetching -----
yfinance==0.2.50
vnstock==3.2.5
beautifulsoup4==4.12.3
requests==2.32.3
lxml==5.3.0
defusedxml==0.7.1
pandas-datareader==0.10.0
certifi>=2024.8.30

# ----- Classical TS -----
statsforecast==1.7.8
prophet==1.1.6

# ----- Machine Learning -----
scikit-learn==1.5.2
xgboost==2.1.2
lightgbm==4.5.0
catboost==1.2.7
optuna==4.0.0
mlforecast==0.13.4

# ----- Deep Learning (CPU stack — install GPU via separate index on Colab) -----
# torch installed via separate command on Windows. Pinned target:
#   pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cpu
torch==2.4.1
neuralforecast==1.7.5

# ----- Foundation models (W4) -----
chronos-forecasting==1.4.1
transformers==4.45.2
# uni2ts (Moirai), timesfm — install on demand in W4

# ----- Conformal prediction -----
mapie==0.9.2

# ----- Tracking & UX -----
mlflow==2.16.2
streamlit==1.40.1

# ----- Plotting -----
matplotlib==3.9.2
seaborn==0.13.2
plotly==5.24.1

# ----- Statsmodels (ADF, ACF, etc.) -----
statsmodels==0.14.4

# ----- XAI -----
shap==0.46.0
